# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/
.vercel
.env*.local
.env*

# Agent/test scratch — never commit these into the tree
.playwright-mcp/
*.patch

# Playwright (issue #35) — test artifacts, never commit
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Stray assets from OTHER projects that share this folder — never belong in the
# game repo (accidentally committed in #157 via `git add -A`; removed in the
# untrack-stray-assets cleanup). DansLab website screenshots + a Nervix runbook.
danslab-*.jpeg
danslab-*.jpg
/docs/runbooks/
