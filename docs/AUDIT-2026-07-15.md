# Reality full audit — 2026-07-15

## Executive verdict

Reality is a strong, unusually well-tested open beta with a coherent product vision, a substantial real-time simulation, fail-closed real-settlement gates, and 1,573 passing unit/integration tests at the current checkpoint. It is **not yet ready for a trusted shared economy or real-value launch**.

The blocking problem is not a missing feature. It is authority: the client store, Neon intent ledger, and Blob Founder Area each own overlapping money, health, time, assets, and transactions. Until there is one atomic server authority, users can mint local state, concurrent requests can lose writes, and normal actions can postpone the simulation clock.

**Release recommendation: REQUEST CHANGES.** Continue open-beta gameplay, but keep payouts, deposits, withdrawals, land sales, death/inheritance, and TON settlement disabled.

## Scope and evidence

Audit baseline: `origin/main` at `69fbd0d`, including merged PR #1020.

Reviewed:

- Product and plan alignment
- Game simulation, economy, health, time, and persistence
- Authentication, authorization, identity, input handling, quotas, and settlement gates
- React UI, map interaction, mobile shell, accessibility, and responsive behavior
- Tests, CI, dependencies, build output, documentation, and delivery workflow

Executed:

- Baseline `npm run verify`: 126 test files and 1,510 tests passed; TypeScript and Vite build passed. Current checkpoint: 139 files / 1,573 tests.
- `npm run lint`: passed with seven existing warnings.
- `npm run test:e2e`: one Chromium first-session test passed.
- Targeted security suites: nine API files and 197 tests passed.
- `npm audit --omit=dev`: zero production advisories.
- Full `npm audit`: ten dev/build-tree advisories (six high, four moderate).
- Browser smoke: desktop 1440×900 and mobile 320×568 loaded; no horizontal overflow at 320px.

Not executed: destructive attacks, production database/blob concurrency tests, real iOS/Android and screen-reader passes, production WAF/header inspection, or enabled real settlement.

## Scorecard

| Area | Rating | Summary |
|---|---:|---|
| Product vision | Strong | Real-time, real-world design laws are clear and differentiated. |
| Core gameplay breadth | Strong | Day loop, market, work, construction, business, map, and founder systems are substantial. |
| Automated unit coverage | Strong | 1,553 passing tests and a fast required verification gate. |
| Server authority | Blocking | Three overlapping authorities; client gameplay remains mutable. |
| Concurrency/data integrity | Blocking at baseline | Blob writes and several quotas used non-atomic read/check/write flows; major quota/authority fixes are merged, but live Neon contention remains unverified. |
| Security foundations | Mixed | Good input allowlists and fail-closed payouts; revocation, new-session expiry, and browser headers are implemented, with production migration pending. |
| UX/mobile | Good beta | Mobile shell is usable, but accessible map interaction and blocked-action clarity lag. |
| E2E/release evidence | Weak | Only a narrow first-session path; first-week lifecycle is not CI-gated. |
| Performance/scale | Needs work | Large mobile bundles and unbounded Blob snapshots/projections. |
| Documentation | Mixed | Strong plans, but README and implementation status contain stale claims. |

## Confirmed findings

### P0 — must resolve before trusted economy

#### P0.1 — No single authoritative economy or simulation

The client mutates and persists local money/body state in `src/store/gameStore.ts:1378-1422` and `src/store/gameStore.ts:3674-3691`. `/api/intent` owns a separate Neon ledger (`api/intent.ts:135-147`, `api/intent.ts:194-206`) but has no production gameplay caller. Founder Area maintains another Blob balance and simulator, and `FounderAreaPanel` builds an in-memory world-sim projection (`src/components/panels/FounderAreaPanel.tsx:1001-1016`).

Impact: local edits can mint game state; formulas and clocks diverge; no component can prove canonical money, ownership, or health.

Action: choose Neon ledger plus canonical `worldSim` as authority, wire all value-changing actions through it, migrate Founder Area, and demote local storage to cache/projection.

#### P0.2 — Founder Area writes are last-write-wins

`api/reality-area.ts:2083-2090` reads a Blob snapshot and `api/reality-area.ts:4308-4319` overwrites it with `allowOverwrite: true` without revision/CAS. Concurrent intents can validate the same funds/capacity and both return success while one update is lost.

PR #1020 now makes sequential same-hour capacity accounting honest, but it cannot solve concurrent same-snapshot requests.

Action: use the existing revision-aware repository and advance/apply/save flow in `src/game/worldSimServer.ts:1455-1483`; persist in Postgres under a per-area transaction/lock with idempotency keys and unique ledger IDs.

#### P0.3 — User actions can postpone simulation indefinitely

`api/reality-area.ts:5641-5647` treats `updatedAt` as the simulation cursor, while build, hire, service, and insurance intents also overwrite it (`api/reality-area.ts:5512`, `5558`, `5703`, `5766`). Any action less than one hour apart can prevent needs, wages, insurance, and hospitalization from advancing.

Action: add `simulatedThroughAt`, keep mutation metadata separate, and always advance to command time before applying an intent.

#### P0.4 — First Neon ledger action accepts a client-chosen seed

`api/intent.ts:194-205` accepts and clamps `predictedBalance`, then uses it as `p_seed`. The day-zero score ceiling reaches $300,000 (`api/_scoresPg.ts:23-29`), while the intended regular-citizen starter credit is $2,500.

Action: create a server-owned genesis ledger row during registration from canonical founder status; remove client seed authority and retain predicted balance only for drift reporting.

### P1 — high priority integrity and security

#### P1.1 — Shared-world placement proves identity, not ownership

`api/world.ts:74-111` authenticates a citizen and validates shape/coordinates, but does not prove catalog item, purchase, inventory ownership, territorial reach, or placement intent.

Action: atomically consume server-owned placeable inventory and validate catalog kind/reach as the committed result of a server intent.

#### P1.2 — Citizen credentials are durable localStorage bearer tokens

At the audit baseline, registration returned a UUID bearer token (`api/register.ts:119-177`) and the database had no session expiry/revocation columns (`scripts/db/schema.sql:10-18`). The current schema now has revocation and new-session expiry; persisted Zustand state still retains the token (`src/store/gameStore.ts:3657-3691`) pending cookie/refresh migration.

Action: introduce expiring/revocable sessions and rotation, preferably Secure/HttpOnly/SameSite cookies with CSRF protection or short-lived access plus protected refresh credentials.

#### P1.3 — Health laws differ across three simulation paths

The plan and main engine use dehydration −6/h, starvation/exhaustion −3/h, and healthy recovery +2/h (`docs/plan/02-HEALTH-SYSTEM.md:80-89`, `src/game/engine.ts:170-183`). `worldSim` uses different penalties/regeneration (`src/game/worldSim.ts:3263-3278`), and Founder Area applies 15 HP per critical need with different hospitalization behavior (`api/reality-area.ts:6168-6179`).

Action: export one approved health policy and add cross-adapter golden tests.

#### P1.4 — Multi-day catch-up silently stops after 24 hours

`api/reality-area.ts:5641-5647` caps a request at 24 ticks; GET performs one pass and returns (`api/reality-area.ts:5018-5035`).

Action: use analytical spans or bounded batches until target time is reached; if continuation is required, report it explicitly.

#### P1.5 — Persisted Founder Area validation is shallow

`api/reality-area.ts:6787-6800` does not validate citizens, timestamps, nested businesses, needs, or transactions before normalization calls `.map()` (`api/reality-area.ts:2140-2155`).

Action: reuse the complete `worldSimCodec` decoder and quarantine/reject invalid snapshots.

#### P1.6 — Expensive Overpass work is public and unauthenticated

At the audit baseline, `api/overpass.ts:28-59` permitted cache-bypass variants and up to three upstream calls totaling 55 seconds; the proxy now rejects non-canonical query shapes and applies a warm-instance per-IP brake, while edge/global circuit-breaking remains a future scale concern.

Action: add edge/IP rate limiting, coordinate bucketing/admission, a global breaker, tighter budgets, and optionally citizen authentication.

#### P1.7 — Map interaction is hidden from assistive technology

`src/components/map/WorldMap.tsx:747` marks the MapLibre container `aria-hidden`, including interactive marker buttons. Owned holdings are clickable `div` elements (`WorldMap.tsx:363-379`) and inspect behavior is pointer/touch-only (`src/components/map/mapQuickInfo.ts:111-131`).

Action: hide only decorative canvas, use semantic buttons, and provide a keyboard/screen-reader marker list or equivalent accessible interaction path.

#### P1.8 — The planned first-week path is not E2E/CI-gated

The plan requires job, needs, study, gather, house, worker, and business lifecycle coverage (`docs/plan/11-DAY-LOOP-IMPLEMENTATION.md:114-118`). `e2e/first-session.spec.ts` covers only citizen creation, food, and work. CI runs Vitest/build only (`.github/workflows/ci.yml:17-21`) and Playwright has Desktop Chrome only.

Action: add deterministic first-week and 320px mobile smoke; gate Chromium in CI and schedule broader browsers if latency is high.

### P2 — should resolve before scale

- **Leaderboard trusts client name/net worth:** `api/leaderboard.ts:54-73`; derive both from canonical citizen/ledger data.
- **Insurance renewal capacity diverges:** Blob renewals ignore capacity (`api/reality-area.ts:5911-5945`) while `worldSim` reserves it.
- **Transaction IDs can collide:** `api/reality-area.ts:5670-5678` uses millisecond/intent/business without uniqueness enforcement.
- **Baseline avatar quota reserved after the billed call:** `api/avatar.ts:180-220`; resolved by the Postgres reservation function before generation.
- **Baseline registration/world quotas were check-then-write:** `api/register.ts:91-117`, `api/world.ts:96-111`, `api/_worldPg.ts:74-90`; registration and world placement now reserve inside Postgres authority boundaries.
- **Telegram identity is not unique:** `telegram_user_id` is nullable/non-unique and registration can bind multiple citizens during init-data validity.
- **Blocked actions are not consistently self-explaining:** Work, construction, and business controls disable without always naming exact missing resources/budget/labor.
- **Mobile sheet semantics conflict:** `MobileHud` declares a non-modal sheet, while `useFocusTrap` prevents keyboard users reaching bottom navigation.
- **Snapshots grow without bound:** transactions/events/reviews append forever; every request downloads the full Blob.
- **Dashboard work becomes O(businesses × transactions):** each business scans the full ledger.
- **Server clock scans Blob areas serially:** the cron path awaits each area one at a time.
- **Mobile bundles are heavy:** build emitted roughly 841 kB main JS, 1,056 kB WorldMap, and 587 kB streetScene before gzip.
- **Dev/build dependency advisories:** ten advisories exist below `@vercel/node`; production dependency audit is clean.
- **Baseline browser security headers were not repository-defined:** resolved by tested Vercel CSP/frame/referrer/permissions policy headers; live response verification remains pending.
- **README is stale:** it still describes a client-only app/no backend and an outdated map stack.

### Local workspace hygiene

- The Git remote contained an embedded GitHub credential. The remote is now token-free and GitHub CLI authentication is used for API operations. The exposed credential must be rotated.
- Ignored Gemini environment files are duplicated and mode `0644`; restrict to `0600`, rotate if exposure is plausible, and remove obsolete backups only through approved secret hygiene.

## Strengths to preserve

- Real payouts, withdrawals, deposits, TON settlement, death, inheritance, and land sale are fail-closed.
- SQL reviewed is parameterized; no confirmed SQL injection was found.
- Intent/API field allowlists prevent server-owned-field smuggling.
- Telegram HMAC/freshness/bot validation and operator token scope/expiry checks are strong.
- Citizen bearer values are randomly generated and only hashes are stored server-side.
- The Neon ledger append serializes per citizen and calculates prices/wages server-side.
- `worldSimServer` already supplies advance-before-intent, revision-aware save, write conflicts, and useful tests.
- The mobile shell has labeled navigation, adequate targets, reduced-motion handling, focus restoration, and no 320px overflow in the smoke check.
- The product plan has clear design laws and measurable D7/contribution success targets.

## Ordered execution plan

The implementation sequence is tracked in `.omx/plans/autopilot-impl.md`. In short:

1. Server-owned ledger genesis and Telegram uniqueness.
2. Atomic revision-safe Founder Area repository and idempotency.
3. Separate simulation cursor and complete catch-up.
4. One health/body policy and one canonical simulation.
5. Authoritative placement, inventory, and leaderboard projections.
6. Expiring sessions, atomic quotas, and browser security policy.
7. Accessible map/actions plus CI-gated first-week/mobile E2E.
8. Snapshot/index/bundle optimization and final launch evidence.

## Completion definition

Reality becomes eligible for a trusted-economy review only when concurrent intent tests, ledger conservation, authoritative ownership, session revocation, complete real-time catch-up, accessible core journeys, and CI-gated first-week E2E all pass. Real settlement remains a separate compliance and custody decision after that engineering gate.

## Autonomous progress — 2026-07-15

- Merged #1023: ledger genesis is server-owned and idempotent (regular 2,500; founder 200,000); client `predictedBalance` no longer establishes money.
- Merged #1024: Founder Area snapshots without a valid `updatedAt` are rejected before simulation or writes.
- Current P0 slice: Founder Area writes now carry the read Blob ETag through conditional `ifMatch` writes, and persisted snapshots track `simulationAt` separately from `updatedAt` with legacy fallback.
- Merged follow-up slice: verified Telegram identity is now claimed in the initial Postgres insert behind a partial unique index; duplicate sessions return deterministic `409 telegram_already_linked` before founder allocation.
- Current validation slice: Founder Area snapshots now validate nested claims, citizens, debts, businesses, transactions, events, and optional simulation cursors before normalization; malformed snapshots are quarantined by the server-clock path.
- Current mutation slice: `buildBusiness` now retries once after a Blob CAS conflict by re-reading and reapplying the pure command against the fresh snapshot; exhausted conflicts return a deterministic retryable 409.
- Follow-up mutation slice: founder service purchases now use the same bounded fresh-read/reapply boundary and return a deterministic retryable 409 after exhausted Blob conflicts.
- Current mutation slice: insurance purchases now use the same bounded fresh-read/reapply boundary while preserving storage-unavailable responses.
- Follow-up mutation slice: worker hiring now uses the same bounded fresh-read/reapply boundary and deterministic retryable conflict response.
- Follow-up mutation slice: server-authorized `advanceHour` now retries once against a fresh snapshot after a Blob CAS conflict.
- Final mutation slice: founder covenant-review recording now retries once against a fresh snapshot after a Blob CAS conflict while preserving evidence-only authority gates.
- Repository-boundary slice: Postgres now defines a Founder Area snapshot table and `reality_save_founder_area` revision/CAS function that raises on stale writes instead of overwriting newer state.
- Repository adapter slice: added an opt-in Postgres adapter that normalizes revisioned reads and calls the database CAS function; runtime remains Blob-backed until migration/cutover is explicitly enabled.
- Runtime integration slice: `REALITY_FOUNDER_AREA_POSTGRES=1` now makes Founder Area reads prefer Postgres snapshots and routes writes through revision CAS, with Blob fallback only for unmigrated areas.
- Migration slice: added a resumable, dry-run-capable Blob backfill that validates snapshots, writes only with expected revision 0, and skips existing Postgres rows without overwriting them.
- Cutover evidence slice: runtime tests now cover Postgres-mode reads without Blob fallback and mutation writes carrying the stored revision into the database CAS function.
- Conflict evidence slice: Postgres revision conflicts now enter the same bounded fresh-read/reapply path as Blob ETag conflicts, with a handler-level regression test.
- Debt mutation slice: founder debt repayment now uses the bounded fresh-read/reapply boundary and returns deterministic retryable conflict responses.
- Final mutable-command slice: operator covenant-review persistence now uses the bounded fresh-read/reapply boundary while preserving target-area validation and evidence-only authority.
- Verified locally: 129 test files, 1,530 tests, TypeScript/Vite build, and lint (7 pre-existing warnings). Production settlement remains disabled.
- All identified Founder Area command handlers now have bounded conflict handling. Operational cutover remains unverified: this session has no `POSTGRES_URL`, `DATABASE_URL`, or Blob token, so the backfill and live Neon concurrency test cannot be run without external credentials.
- Tooling fix: dry-run backfills no longer require a Postgres URL, so Blob snapshot validation can proceed independently before write credentials are introduced.
- Authority fix: Postgres-mode reads now fail closed when a database row exists but its nested snapshot is malformed; they no longer fall through to potentially stale Blob state.
- Simulation routing fix: server-clock batch ticks now read and persist through the Postgres repository when the migration flag is enabled, instead of mutating only legacy Blob snapshots.
- Discovery routing fix: server-clock and covenant-review batch scans now enumerate the Postgres snapshot table when enabled, so newly claimed Postgres-only areas cannot be skipped by legacy Blob listing.
- Catch-up correctness fix: Founder Area reads and server-clock commands now persist successive bounded 24-hour batches until the simulation cursor reaches command time, with an explicit one-year safety ceiling instead of silent truncation.
- Health authority fix: Founder Area, `worldSim`, and the canonical life engine now share one triage policy (dehydration −6/hour, hunger or exhaustion −3/hour, healthy recovery +2/hour) with cross-adapter regression coverage.
- Placement boundary fix: shared-world placement now validates catalog identity and requires exact server-owned ledger-backed inventory before the atomic charge/write.
- Placement transaction fix: Postgres now performs the server balance check, asset insert, and `world.place` debit inside one advisory-locked, idempotent function; failed writes cannot leave an uncharged asset or a charge without an asset.
- Placement quota fix: the daily world-placement cap is now enforced inside that same locked function, closing the concurrent pre-check race.
- Placement idempotency fix: replaying an existing asset ID may move the same catalog identity, but cannot change its item or kind without a new authoritative purchase.
- Overpass admission fix: the public map proxy now rejects unknown query keys and repeated coordinate values before any upstream call, preventing trivial cache-bypass variants while preserving the canonical coordinate-only contract.
- Browser boundary fix: Vercel now emits tested CSP, anti-framing, MIME-sniffing, referrer, and Permissions-Policy headers compatible with Google Identity, OpenFreeMap/MapLibre, Overpass, fonts, and service-worker workers.
- Registration abuse fix: the per-IP/day registration brake now reserves attempts through a Postgres upsert/function, eliminating the concurrent Blob list-then-write race; safety failures remain fail-closed.
- Session boundary fix: citizen verification now rejects server-revoked bearer tokens and expired new sessions, with `/api/revoke-session` backed by `token_revoked_at`; legacy rows without `token_expires_at` remain valid for compatibility.
- Session transport fix: registration now issues a 30-day Secure/HttpOnly/SameSite session cookie and explicit revocation clears it, establishing the cookie migration path while retaining the legacy response token for compatibility.
- Cookie-auth boundary fix: the central intent endpoint now accepts the session cookie only with a matching readable CSRF cookie/header pair; legacy body-token callers remain supported.
- Cookie-auth expansion: world placement, avatar generation, and cloud-save now share the same CSRF-protected cookie migration path while preserving legacy body-token compatibility.
- Leaderboard auth expansion: score submissions now use the same CSRF-protected cookie migration path while preserving legacy body-token compatibility.
- Logout auth expansion: explicit session revocation now supports CSRF-protected cookie authentication while preserving the legacy body-token path.
- Google auth expansion: explicit `action: "link"` now supports CSRF-protected cookie linking without changing the existing restore contract.
- Cutover preflight: `node scripts/cutover-preflight.mjs` now reports missing database/blob authority without contacting services or exposing credential values; it remains red in this session because both credential classes are absent.
- Live CAS probe: `scripts/founder-area-cas-probe.mjs` now provides an explicit, opt-in concurrent revision/CAS proof for a dedicated test citizen/area; it refuses writes unless `REALITY_CUTOVER_ALLOW_WRITE=1`.
- Overpass abuse fix: the public proxy now applies a bounded warm-instance per-IP admission brake plus a Postgres-backed cross-instance IP/global rolling-window admission function before upstream mirror work; a true edge/WAF limit remains deployment work.
- E2E release fix: CI now runs the built-app Playwright suite after unit/build verification, covering the first-session journey and a deterministic 320px mobile overflow check.
- Migration evidence fix: `node scripts/db-migrate.mjs --dry-run` now parses the exact idempotent schema and verifies required Founder Area, placement, registration-quota, and session objects without credentials or network access; it does not claim production application.
- Avatar cost-boundary fix: per-citizen and global daily generation budgets now reserve atomically in Postgres before any billed OpenAI request; Blob markers remain compatibility/audit artifacts only.
- Intent integrity fix: the hourly intent limit now has a final check inside the per-citizen Postgres advisory lock, closing the concurrent check-then-append bypass while preserving the cheap handler pre-check.
- Session transport fix: the client now keeps the bearer only in memory/sessionStorage compatibility state, strips it from the durable game save, sends cookie-auth requests with CSRF proof, and bootstraps returning devices without re-registering an existing identity.
- Intent authority fix: economic appends now accept a bounded idempotency key, enforce a unique citizen/ledger boundary inside the advisory lock, and replay the original result on retry instead of charging or paying twice.
- Transaction identity fix: Founder Area transaction IDs now preserve readable timestamp IDs while deterministically suffixing same-base collisions, so same-millisecond commands cannot overwrite one another in downstream projections.
- Leaderboard identity fix: score submissions now derive the displayed name from the authoritative citizens row instead of accepting a client-supplied name; the existing net-worth plausibility cap remains fail-closed.
- Migration evidence fix: the credential-free schema dry-run now explicitly checks the authoritative intent append function and idempotency index in addition to Founder Area, placement, registration, and session objects.
- Current local gate: 142 test files and 1,583 tests pass with build and lint; the remaining evidence gap is production migration plus live Neon contention, not local verification.
- Documentation truth fix: README now describes the Vercel API, Neon Postgres, staged Founder Area authority, and Blob compatibility layers instead of the obsolete client-only/no-backend beta.
- Insurance capacity fix: Founder Area renewals now consume the same hourly insurer capacity budget as initial policies and `worldSim`, lapsing rather than charging when capacity is exhausted.
- Inventory authority fix: shared-world placement now requires a server-owned, ledger-backed purchase for the exact catalog item; missing inventory is rejected inside the same advisory-locked transaction as the asset write and charge.
- Map accessibility fix: the decorative MapLibre canvas is now paired with a semantic keyboard/screen-reader holdings list that focuses and opens each owned property.
- Backfill preflight fix: Founder Area migration now reports the missing Blob credential directly before invoking the SDK; dry-run still requires read access to the source snapshots and does not claim to be credential-free.
- Backfill safety fix: non-dry-run Founder Area migration now requires an explicit `FOUNDER_AREA_MIGRATION_ALLOW_WRITE=1` opt-in before reading credentials or contacting Blob/Postgres; accidental writable invocations fail closed.
- Founder Area session fix: `/api/reality-area` now accepts the Secure/HttpOnly citizen session cookie and requires the readable CSRF cookie/header pair for cookie-authenticated mutations, while preserving legacy body-token callers.
- Returning-device fix: Founder Area client requests now use the session cookie and CSRF proof when the compatibility bearer is absent, so cookie-only sessions do not appear online but become unable to use Founder Area actions.
- Credential transport fix: Founder Area client bridges now trim citizen IDs and bearer tokens before transport, preventing whitespace-padded credentials from being rejected or logged inconsistently.
- Claim/review transport fix: Founder Area client bridges now constrain malformed claim sources to `manual`, trim operator review identifiers, and omit blank notes before authority transport.
- Debt identity fix: pure world simulation now trims requested debt IDs, matches padded imported debt records without rewriting their stored IDs, and rejects blank IDs before mutation.
- Worker identity fix: pure world simulation now trims requested hireWorker business IDs, matches padded imported businesses without rewriting their stored IDs, and preserves assigned job references.
- Queue transport fix: Founder Covenant review queue requests now trim cursors and omit blank, non-finite, or non-positive limit/page values before authority transport.
- Telegram client auth fix: successful Mini App responses with `authDate` more than 60 seconds ahead of the client clock are rejected as invalid sessions.
- Telegram bot-account coverage fix: registration rejects verified bot users before reserving capacity, and the client maps the `bot_user` verifier code to `invalid_session`.
- Covenant urgency fix: read-only Founder Covenant queue ordering now gives monthly-due reviews a higher priority than weekly-due reviews without enabling approvals or automated actions.
- Evidence backlog fix: read-only Founder Covenant queue totals now report manual evidence gaps for population growth, external contribution, and ideas/feedback across scanned founders.
- Queue presenter fix: the read-only Founder Covenant operator queue now surfaces those per-category evidence gaps as visible warning/stable totals chips, so operators can see the backlog before opening individual rows.
- CAS resurrection fix: the Founder Area Postgres save function now separates initial revision-0 creation from non-zero updates, so a deleted row cannot be recreated from a stale non-zero revision.
- Corrupt-snapshot observability fix: malformed authoritative Postgres Founder Area rows still fail closed and now emit a bounded diagnostic containing only the citizen ID and revision, making repair incidents distinguishable from unclaimed areas.
- Bearer-at-rest fix: save migration and cloud-save ingestion now strip legacy citizen bearer tokens before returning or storing persisted state, closing the pre-fix local/blob retention path.
- Registration identity fix: the Postgres registration brake now prefers Vercel's trusted forwarding header and otherwise hashes the rightmost forwarded hop, preventing spoofed XFF prefixes from bypassing the per-IP/day cap.
- Cloud-save ordering fix: Postgres now accepts only monotonically newer save timestamps as the authoritative snapshot, while Blob remains a compatibility mirror and Google restore reads the ordered snapshot first.
- Client session fix: explicit local account reset now best-effort revokes the server bearer session before clearing persisted local state, with immediate offline-safe cleanup.
- Remaining P0 work: run the backfill against the production database, add a live concurrent Postgres test, and complete operational cutover evidence; settlement remains disabled until then.
